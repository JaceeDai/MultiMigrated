pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
//    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS) //TODO 注释掉，因为报 "ivy" 错误
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "Simple login"
include ':app'
include ':shared'
